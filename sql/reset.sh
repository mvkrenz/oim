echo "TRUNCATE mesh_config_group" | mysql -u root oim
echo "TRUNCATE mesh_config_oim_member" | mysql -u root oim
echo "TRUNCATE mesh_config_wlcg_member" | mysql -u root oim
echo "TRUNCATE mesh_config_param" | mysql -u root oim
echo "TRUNCATE mesh_config_test" | mysql -u root oim
echo "TRUNCATE mesh_config_contact" | mysql -u root oim
echo "TRUNCATE mesh_config" | mysql -u root oim
